# poly_onchain 依赖
# 安装: conda activate python3.12 && pip install -r requirements.txt

# 核心依赖
web3>=6.0.0              # Polygon RPC 交互
requests>=2.28.0         # HTTP 请求 (Gamma API)
pandas>=2.0.0            # 数据处理
pyarrow>=12.0.0          # Parquet 读写

# 数据库
duckdb>=1.0.0            # 嵌入式分析数据库

# API 服务
fastapi>=0.110.0         # Web 框架
uvicorn>=0.27.0          # ASGI 服务器
pydantic>=2.0.0          # 数据验证（fastapi 依赖）

# 可选依赖
eth-abi>=4.0.0           # ABI 编解码（web3 已包含，单独列出以确保版本）
